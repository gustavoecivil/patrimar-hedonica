# Dependências
node_modules/

# Segredos e configuração local
.env
.env.*
!.env.example

# Netlify (build/cache local)
.netlify/

# Config local do Claude Code (não deve ser versionada)
.claude/settings.local.json

# Zona restrita: planilhas e dados privados/reais da Patrimar/Rodolfo.
# Nada dentro desta pasta pode ser commitado neste repositório público —
# ver docs/04-DECISIONS.md (D4) e docs/03-ROADMAP.md.
data/restricted/
